/** Muted shell — matches marketing homepage / login chrome */
export const LANDING_SHELL_BACKGROUND = [
  "radial-gradient(900px 480px at 50% -8%, rgba(255,255,255,0.035) 0%, transparent 58%)",
  "var(--govai-bg-app)",
].join(", ");
