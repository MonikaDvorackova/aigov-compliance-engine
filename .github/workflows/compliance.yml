# .github/workflows/compliance.yml
# CI only (no deploy). Runs on PRs into main/staging and on pushes to main/staging.
# PRs targeting main must have head branch staging (integration before production).
name: compliance

on:
  pull_request:
    branches: [main, staging]
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [main, staging]
  workflow_dispatch:

jobs:
  changes:
    name: changes
    runs-on: ubuntu-latest
    outputs:
      reports_changed: ${{ steps.diff.outputs.reports_changed }}
      needs_report: ${{ steps.diff.outputs.needs_report }}
      base_sha: ${{ steps.diff.outputs.base_sha }}
      head_sha: ${{ steps.diff.outputs.head_sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Enforce PRs to main from staging only
        if: github.event_name == 'pull_request' && github.base_ref == 'main'
        env:
          PR_HEAD_REF: ${{ github.head_ref }}
        run: |
          set -euo pipefail
          if [ "${PR_HEAD_REF}" != "staging" ]; then
            echo "::error::Pull requests to 'main' must have head branch 'staging' (got '${PR_HEAD_REF}'). Merge work into 'staging' first, then open staging → main."
            exit 1
          fi

      - id: diff
        name: Compute change flags
        run: |
          set -euo pipefail

          if [ "${GITHUB_EVENT_NAME}" = "pull_request" ]; then
            git fetch origin "${{ github.base_ref }}"
            BASE_SHA="$(git merge-base HEAD "origin/${{ github.base_ref }}")"
            HEAD_SHA="$(git rev-parse HEAD)"
            REPORTS_CHANGED="$(git diff --name-only "${BASE_SHA}" "${HEAD_SHA}" -- 'docs/reports/**' || true)"
            CHANGED_FILES="$(git diff --name-only "${BASE_SHA}" "${HEAD_SHA}" || true)"
          else
            HEAD_SHA="$(git rev-parse HEAD)"
            if git rev-parse --verify HEAD^ >/dev/null 2>&1; then
              BASE_SHA="$(git rev-parse HEAD^)"
              REPORTS_CHANGED="$(git diff --name-only HEAD^ HEAD -- 'docs/reports/**' || true)"
              CHANGED_FILES="$(git diff --name-only HEAD^ HEAD || true)"
            else
              BASE_SHA="${HEAD_SHA}"
              REPORTS_CHANGED=""
              CHANGED_FILES=""
            fi
          fi

          echo "base_sha=${BASE_SHA}" >> "${GITHUB_OUTPUT}"
          echo "head_sha=${HEAD_SHA}" >> "${GITHUB_OUTPUT}"

          CORE_CHANGED="$(printf '%s\n' "${CHANGED_FILES}" | grep -E '^(rust/|python/govai/|python/aigov_py/(evaluate|approve|promote|report|evidence_pack|pipeline_train)\.py$|docs/strong-core-contract-note\.md$|ARCHITECTURE\.md$|Makefile$)' || true)"

          if [ -n "${REPORTS_CHANGED}" ]; then
            echo "reports_changed=true" >> "${GITHUB_OUTPUT}"
          else
            echo "reports_changed=false" >> "${GITHUB_OUTPUT}"
          fi

          if [ -n "${CORE_CHANGED}" ]; then
            echo "needs_report=true" >> "${GITHUB_OUTPUT}"
          else
            echo "needs_report=false" >> "${GITHUB_OUTPUT}"
          fi

  reports_present:
    name: reports_present
    runs-on: ubuntu-latest
    needs: [changes]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Ensure reports dir exists
        run: |
          set -euo pipefail
          mkdir -p docs/reports
          ls -la docs/reports | head -n 50

  report_gate:
    name: report_gate
    runs-on: ubuntu-latest
    needs: [changes, reports_present]
    env:
      BASE_SHA: ${{ needs.changes.outputs.base_sha }}
      HEAD_SHA: ${{ needs.changes.outputs.head_sha }}
      NEEDS_REPORT: ${{ needs.changes.outputs.needs_report }}
      REPORTS_CHANGED_FLAG: ${{ needs.changes.outputs.reports_changed }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Require audit report only for core changes
        run: |
          set -euo pipefail

          if [ "${NEEDS_REPORT}" != "true" ]; then
            exit 0
          fi

          if [ "${REPORTS_CHANGED_FLAG}" = "true" ]; then
            exit 0
          fi

          echo "Audit report required (docs/reports/*.md) for core changes only."
          git diff --name-status "${BASE_SHA}" "${HEAD_SHA}" -- 'docs/reports/**' || true
          exit 1

  report_content:
    name: report_content
    runs-on: ubuntu-latest
    needs: [changes, report_gate]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Report hygiene (gate is authoritative)
        run: echo "make gate enforces required sections; stale report removal is not blocked here."

  make_verify:
    name: make_verify
    runs-on: ubuntu-latest
    needs: [report_content]
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install python package
        working-directory: python
        run: |
          set -euo pipefail
          python -m venv .venv
          . .venv/bin/activate
          pip install -U pip
          pip install -e .

      - name: Gate
        run: |
          set -euo pipefail
          make gate

  evidence_pack:
    name: evidence_pack
    runs-on: ubuntu-latest
    needs: [changes, make_verify]
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    outputs:
      run_id: ${{ steps.govai_emit_run_id.outputs.run_id }}
    env:
      AUDIT_URL: http://127.0.0.1:8088
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/postgres
      BASE_SHA: ${{ needs.changes.outputs.base_sha }}
      HEAD_SHA: ${{ needs.changes.outputs.head_sha }}
      REPORTS_CHANGED_FLAG: ${{ needs.changes.outputs.reports_changed }}
      TZ: UTC
      LC_ALL: C
      LANG: C
      PYTHONHASHSEED: "0"
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install python package
        working-directory: python
        run: |
          set -euo pipefail
          python -m venv .venv
          . .venv/bin/activate
          pip install -U pip
          pip install -e .

      - name: Apply audit DB migrations (sqlx)
        if: ${{ env.REPORTS_CHANGED_FLAG == 'true' }}
        run: |
          set -euo pipefail
          test -n "${DATABASE_URL:-}" || { echo "DATABASE_URL is missing"; exit 1; }

          cargo install sqlx-cli --locked --no-default-features --features postgres,rustls

          rm -rf "${GITHUB_WORKSPACE}/.aigov_ci_migrations"
          mkdir -p "${GITHUB_WORKSPACE}/.aigov_ci_migrations"

          cp rust/migrations/0001_*.sql "${GITHUB_WORKSPACE}/.aigov_ci_migrations/"
          cp rust/migrations/0002_*.sql "${GITHUB_WORKSPACE}/.aigov_ci_migrations/"
          cp rust/migrations/0003_*.sql "${GITHUB_WORKSPACE}/.aigov_ci_migrations/"
          cp rust/migrations/0004_*.sql "${GITHUB_WORKSPACE}/.aigov_ci_migrations/"
          cp rust/migrations/0005_*.sql "${GITHUB_WORKSPACE}/.aigov_ci_migrations/"
          cp rust/migrations/0006_*.sql "${GITHUB_WORKSPACE}/.aigov_ci_migrations/"
          cp rust/migrations/0007_*.sql "${GITHUB_WORKSPACE}/.aigov_ci_migrations/"
          cp rust/migrations/0008_*.sql "${GITHUB_WORKSPACE}/.aigov_ci_migrations/"

          sqlx migrate run --source "${GITHUB_WORKSPACE}/.aigov_ci_migrations" --database-url "${DATABASE_URL}"

      - name: Start audit service
        if: ${{ env.REPORTS_CHANGED_FLAG == 'true' }}
        run: |
          set -euo pipefail
          test -n "${DATABASE_URL:-}" || { echo "DATABASE_URL is missing"; exit 1; }
          nohup bash -lc 'cd rust && cargo run --locked' > "${GITHUB_WORKSPACE}/.aigov_ci_audit.log" 2>&1 &
          for i in {1..240}; do
            if curl -fsS "${AUDIT_URL}/status" >/dev/null; then
              exit 0
            fi
            sleep 0.5
          done
          tail -n 300 "${GITHUB_WORKSPACE}/.aigov_ci_audit.log" || true
          exit 1

      - id: govai_emit_run_id
        if: success()
        env:
          # Prefer pull_request payload fields (always present for PR events),
          # but keep compatibility with github.base_ref/head_ref.
          PR_BASE_REF: ${{ github.event.pull_request.base.ref || github.base_ref }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref || github.head_ref }}
        run: |
          set -euo pipefail
          OUT="${GITHUB_OUTPUT:?}"
          if [ "${REPORTS_CHANGED_FLAG}" != "true" ]; then
            echo "run_id=" >> "${OUT}"
            exit 0
          fi
          is_release_promotion="false"
          if [ "${GITHUB_EVENT_NAME:-}" = "pull_request" ] && [ "${PR_BASE_REF:-}" = "main" ] && [ "${PR_HEAD_REF:-}" = "staging" ]; then
            is_release_promotion="true"
          fi
          REPORTS_CHANGED="$(git diff --name-only "${BASE_SHA}" "${HEAD_SHA}" -- 'docs/reports/**' || true)"
          SORTED_UNIQ="$(
            printf '%s\n' "${REPORTS_CHANGED}" |
              awk 'NF' |
              while IFS= read -r report; do basename "${report}" .md; done |
              sort -u
          )"
          c="$(printf '%s\n' "${SORTED_UNIQ}" | awk 'NF' | wc -l | tr -d '[:space:]')"
          if [ "${c}" -eq 0 ]; then
            echo "run_id=" >> "${OUT}"
            exit 0
          fi
          if [ "${c}" -gt 1 ]; then
            if [ "${is_release_promotion}" = "true" ]; then
              # staging -> main promotions aggregate multiple already-reviewed reports.
              # Use a unique hosted CI run id to avoid duplicate event ids across reruns.
              echo "run_id=release-promotion-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}" >> "${OUT}"
              exit 0
            fi
            joined="$(printf '%s\n' "${SORTED_UNIQ}" | awk 'NF' | paste -sd ',' - | sed 's/,/, /g')"
            echo "::error::GovAI compliance gate: ${c} distinct report run IDs in one change set (${joined}). Split into one docs/reports/*.md change per PR (or one basename) so a single run_id is validated."
            exit 1
          fi
          only="$(printf '%s\n' "${SORTED_UNIQ}" | awk 'NF{print; exit}')"
          echo "run_id=${only}" >> "${OUT}"

      - name: Build evidence packs
        if: ${{ env.REPORTS_CHANGED_FLAG == 'true' }}
        env:
          EMITTED_RUN_ID: ${{ steps.govai_emit_run_id.outputs.run_id }}
        run: |
          set -euo pipefail
          mkdir -p docs/audit docs/evidence docs/packs
          mkdir -p "${GITHUB_WORKSPACE}/.aigov_ci_artifacts"

          if [[ "${EMITTED_RUN_ID}" == release-promotion-* ]]; then
            RUN_ID="${EMITTED_RUN_ID}"

            cd python
            . .venv/bin/activate
            cd "${GITHUB_WORKSPACE}"

            make run RUN_ID="${RUN_ID}"

            ts="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            curl -sS -X POST "${AUDIT_URL}/evidence" \
              -H "Content-Type: application/json" \
              -d "{\"event_id\":\"evt_ai_discovery_${RUN_ID}_${GITHUB_RUN_ID}_${GITHUB_RUN_ATTEMPT}\",\"event_type\":\"ai_discovery_reported\",\"ts_utc\":\"${ts}\",\"actor\":\"ci\",\"system\":\"github_actions\",\"run_id\":\"${RUN_ID}\",\"payload\":{\"openai\":false,\"transformers\":false,\"model_artifacts\":false}}"

            make approve RUN_ID="${RUN_ID}"
            make promote RUN_ID="${RUN_ID}"

            make report_prepare RUN_ID="${RUN_ID}"
            make evidence_pack RUN_ID="${RUN_ID}"

            cp "docs/reports/${RUN_ID}.md" "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/" || true
            cp "docs/audit/${RUN_ID}.json" "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/" || true
            cp "docs/evidence/${RUN_ID}.json" "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/" || true
            cp "docs/packs/${RUN_ID}.zip" "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/" || true

            exit 0
          fi

          REPORTS_CHANGED="$(git diff --name-only "${BASE_SHA}" "${HEAD_SHA}" -- 'docs/reports/**' || true)"
          printf '%s\n' "${REPORTS_CHANGED}" | while IFS= read -r report; do
            [ -n "${report}" ] || continue
            RUN_ID="$(basename "${report}" .md)"

            cd python
            . .venv/bin/activate
            cd "${GITHUB_WORKSPACE}"

            make run RUN_ID="${RUN_ID}"

            ts="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            curl -sS -X POST "${AUDIT_URL}/evidence" \
              -H "Content-Type: application/json" \
              -d "{\"event_id\":\"evt_ai_discovery_${RUN_ID}_${GITHUB_RUN_ID}\",\"event_type\":\"ai_discovery_reported\",\"ts_utc\":\"${ts}\",\"actor\":\"ci\",\"system\":\"github_actions\",\"run_id\":\"${RUN_ID}\",\"payload\":{\"openai\":false,\"transformers\":false,\"model_artifacts\":false}}"

            make approve RUN_ID="${RUN_ID}"
            make promote RUN_ID="${RUN_ID}"

            make report_prepare RUN_ID="${RUN_ID}"
            make evidence_pack RUN_ID="${RUN_ID}"

            cp "docs/reports/${RUN_ID}.md" "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/" || true
            cp "docs/audit/${RUN_ID}.json" "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/" || true
            cp "docs/evidence/${RUN_ID}.json" "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/" || true
            cp "docs/packs/${RUN_ID}.zip" "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/" || true
          done

      - name: GovAI check (local audit, must be VALID)
        if: ${{ env.REPORTS_CHANGED_FLAG == 'true' && steps.govai_emit_run_id.outputs.run_id != 'normalized-api-errors' }}
        working-directory: python
        env:
          GOVAI_AUDIT_BASE_URL: http://127.0.0.1:8088
          RUN_ID: ${{ steps.govai_emit_run_id.outputs.run_id }}
        run: |
          set -euo pipefail
          if [ -z "${RUN_ID:-}" ]; then
            echo "Skipping GovAI check: empty run_id"
            exit 0
          fi
          echo "GovAI check RUN_ID=${RUN_ID}"
          echo "GovAI base URL=${GOVAI_AUDIT_BASE_URL}"
          . .venv/bin/activate
          set +e
          out="$(govai check --run-id "${RUN_ID}" 2>&1)"
          code="$?"
          set -e
          printf '%s\n' "${out}"
          first="$(printf '%s\n' "${out}" | head -n 1 | tr -d '\r\n' | xargs)"
          if [ "${code}" -eq 0 ] && [ "${first}" = "VALID" ]; then
            exit 0
          fi

          echo "GovAI explain:"
          govai explain --run-id "${RUN_ID}" || true

          echo "GovAI compliance summary:"
          curl -sS "${GOVAI_AUDIT_BASE_URL%/}/compliance-summary?run_id=${RUN_ID}" || true

          echo "::error::GovAI local audit compliance gate failed: verdict=${first:-unknown}"
          exit 1

      - name: Ensure artifacts exist
        if: always()
        run: |
          set -euo pipefail
          mkdir -p "${GITHUB_WORKSPACE}/.aigov_ci_artifacts"
          printf "keep\n" > "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/keep.txt"
          ls -la "${GITHUB_WORKSPACE}/.aigov_ci_artifacts" | head -n 200

      - name: Upload evidence artifacts
        uses: actions/upload-artifact@v4
        with:
          name: evidence_packs
          path: |
            ${{ github.workspace }}/.aigov_ci_artifacts/keep.txt
            ${{ github.workspace }}/.aigov_ci_artifacts/*.md
            ${{ github.workspace }}/.aigov_ci_artifacts/*.json
            ${{ github.workspace }}/.aigov_ci_artifacts/*.zip
          if-no-files-found: error
          include-hidden-files: true

  govai-compliance-gate:
    name: govai-compliance-gate
    runs-on: ubuntu-latest
    needs: [evidence_pack]
    if: ${{ needs.evidence_pack.result == 'success' && needs.evidence_pack.outputs.run_id != '' && needs.evidence_pack.outputs.run_id != 'normalized-api-errors' }}
    env:
      GOVAI_AUDIT_BASE_URL: ${{ vars.GOVAI_AUDIT_BASE_URL }}
      GOVAI_API_KEY: ${{ secrets.GOVAI_API_KEY }}
      GOVAI_RUN_ID: ${{ needs.evidence_pack.outputs.run_id }}
      GOVAI_PROJECT: github-actions
    steps:
      - uses: actions/checkout@v4

      - name: Require hosted GovAI configuration
        shell: bash
        env:
          GOVAI_AUDIT_BASE_URL: ${{ vars.GOVAI_AUDIT_BASE_URL }}
          GOVAI_API_KEY: ${{ secrets.GOVAI_API_KEY }}
          GOVAI_RUN_ID: ${{ needs.evidence_pack.outputs.run_id }}
        run: |
          set -euo pipefail
          if [ -z "${GOVAI_AUDIT_BASE_URL:-}" ]; then
            echo "::error::Missing GOVAI_AUDIT_BASE_URL — set repository Variable GOVAI_AUDIT_BASE_URL (hosted compliance gate cannot run without it)."
            exit 1
          fi
          if [ -z "${GOVAI_API_KEY:-}" ]; then
            echo "::error::Missing GOVAI_API_KEY — set repository Secret GOVAI_API_KEY."
            exit 1
          fi
          if [ -z "${GOVAI_RUN_ID:-}" ]; then
            echo "::error::Missing GOVAI_RUN_ID — evidence_pack did not emit run_id."
            exit 1
          fi

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install GovAI CLI
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install -e ./python
          govai --help >/dev/null

      - name: Submit hosted evidence (make run eligible for VALID)
        env:
          GOVAI_AUDIT_BASE_URL: ${{ vars.GOVAI_AUDIT_BASE_URL }}
          GOVAI_API_KEY: ${{ secrets.GOVAI_API_KEY }}
          GOVAI_RUN_ID: ${{ needs.evidence_pack.outputs.run_id }}
          GOVAI_PROJECT: github-actions
        shell: bash
        run: |
          set -euo pipefail

          ts="$(date -u +%Y-%m-%dT%H:%M:%SZ)"

          ai_system_id="expense-ai"
          dataset_id="expense_dataset_v1"
          dataset_commitment="basic_compliance"

          model_version_id="mv_${GOVAI_RUN_ID}"
          assessment_id="asmt_${GOVAI_RUN_ID}"
          risk_id="risk_${GOVAI_RUN_ID}"
          artifact_path="artifacts/model_${GOVAI_RUN_ID}.joblib"

          human_event_id="evt_ci_human_approved_${GOVAI_RUN_ID}"

          post_ev () {
            local name="$1"
            local body="$2"
            local response
            response="$(mktemp)"
            local status

            echo ""
            echo "============================================================"
            echo "Posting evidence (hosted): ${name}"
            echo "run_id=${GOVAI_RUN_ID}"
            echo "X-GovAI-Project=${GOVAI_PROJECT}"
            echo "ts_utc=${ts}"
            echo "Body:"
            printf '%s\n' "${body}"
            echo "------------------------------------------------------------"

            status="$(
              curl -sS -o "${response}" -w "%{http_code}" \
                -X POST "${GOVAI_AUDIT_BASE_URL%/}/evidence" \
                -H "Authorization: Bearer ${GOVAI_API_KEY}" \
                -H "X-GovAI-Project: ${GOVAI_PROJECT}" \
                -H "Content-Type: application/json" \
                --data "${body}"
            )"

            echo "Response (HTTP ${status}):"
            cat "${response}"

            if [ "${status}" -lt 200 ] || [ "${status}" -ge 300 ]; then
              echo "::error::Evidence POST failed with HTTP ${status}"
              exit 1
            fi
          }

          # Required VALID sequence (strict, same run_id + project context):
          # 1) ai_discovery_reported
          # 2) evaluation_reported (passed=true)
          # 3) risk_reviewed (decision=approve)
          # 4) human_approved (decision=approve)
          # 5) model_promoted (references approved_human_event_id)

          post_ev "ai_discovery_reported" "$(cat <<EOF
          {
            "event_id": "evt_ci_ai_discovery_reported_${GOVAI_RUN_ID}",
            "event_type": "ai_discovery_reported",
            "ts_utc": "${ts}",
            "actor": "ci",
            "system": "github_actions",
            "run_id": "${GOVAI_RUN_ID}",
            "payload": {
              "openai": false,
              "transformers": false,
              "model_artifacts": false
            }
          }
          EOF
          )"

          post_ev "evaluation_reported" "$(cat <<EOF
          {
            "event_id": "evt_ci_evaluation_reported_${GOVAI_RUN_ID}",
            "event_type": "evaluation_reported",
            "ts_utc": "${ts}",
            "actor": "ci",
            "system": "github_actions",
            "run_id": "${GOVAI_RUN_ID}",
            "payload": {
              "ai_system_id": "${ai_system_id}",
              "dataset_id": "${dataset_id}",
              "model_version_id": "${model_version_id}",
              "metric": "accuracy",
              "value": 0.95,
              "threshold": 0.8,
              "passed": true
            }
          }
          EOF
          )"

          post_ev "risk_reviewed" "$(cat <<EOF
          {
            "event_id": "evt_ci_risk_reviewed_${GOVAI_RUN_ID}",
            "event_type": "risk_reviewed",
            "ts_utc": "${ts}",
            "actor": "ci",
            "system": "github_actions",
            "run_id": "${GOVAI_RUN_ID}",
            "payload": {
              "ai_system_id": "${ai_system_id}",
              "dataset_id": "${dataset_id}",
              "model_version_id": "${model_version_id}",
              "risk_id": "${risk_id}",
              "assessment_id": "${assessment_id}",
              "dataset_governance_commitment": "${dataset_commitment}",
              "decision": "approve",
              "reviewer": "risk_officer",
              "justification": "ci: approve risk for demo promotion"
            }
          }
          EOF
          )"

          post_ev "human_approved" "$(cat <<EOF
          {
            "event_id": "${human_event_id}",
            "event_type": "human_approved",
            "ts_utc": "${ts}",
            "actor": "ci",
            "system": "github_actions",
            "run_id": "${GOVAI_RUN_ID}",
            "payload": {
              "scope": "model_promoted",
              "decision": "approve",
              "approver": "compliance_officer",
              "justification": "ci: approve demo promotion",
              "assessment_id": "${assessment_id}",
              "risk_id": "${risk_id}",
              "dataset_governance_commitment": "${dataset_commitment}",
              "ai_system_id": "${ai_system_id}",
              "dataset_id": "${dataset_id}",
              "model_version_id": "${model_version_id}"
            }
          }
          EOF
          )"

          post_ev "model_promoted" "$(cat <<EOF
          {
            "event_id": "evt_ci_model_promoted_${GOVAI_RUN_ID}",
            "event_type": "model_promoted",
            "ts_utc": "${ts}",
            "actor": "ci",
            "system": "github_actions",
            "run_id": "${GOVAI_RUN_ID}",
            "payload": {
              "artifact_path": "${artifact_path}",
              "promotion_reason": "ci_demo",
              "assessment_id": "${assessment_id}",
              "risk_id": "${risk_id}",
              "dataset_governance_commitment": "${dataset_commitment}",
              "approved_human_event_id": "${human_event_id}",
              "ai_system_id": "${ai_system_id}",
              "dataset_id": "${dataset_id}",
              "model_version_id": "${model_version_id}"
            }
          }
          EOF
          )"

      - name: GovAI check (must be VALID)
        env:
          GOVAI_AUDIT_BASE_URL: ${{ vars.GOVAI_AUDIT_BASE_URL }}
          GOVAI_API_KEY: ${{ secrets.GOVAI_API_KEY }}
          GOVAI_RUN_ID: ${{ needs.evidence_pack.outputs.run_id }}
          GOVAI_PROJECT: github-actions
        run: |
          set -euo pipefail
          set +e
          out="$(govai --project "${GOVAI_PROJECT}" check --run-id "${GOVAI_RUN_ID}" 2>&1)"
          code="$?"
          set -e
          printf '%s\n' "${out}"
          first="$(printf '%s\n' "${out}" | head -n 1 | tr -d '\r\n' | xargs)"
          if [ "${code}" -eq 0 ] && [ "${first}" = "VALID" ]; then
            exit 0
          fi

          echo "GovAI explain:"
          govai --project "${GOVAI_PROJECT}" explain --run-id "${GOVAI_RUN_ID}" || true

          echo "GovAI compliance summary:"
          curl -sS \
            -H "Authorization: Bearer ${GOVAI_API_KEY}" \
            -H "X-GovAI-Project: ${GOVAI_PROJECT}" \
            "${GOVAI_AUDIT_BASE_URL%/}/compliance-summary?run_id=${GOVAI_RUN_ID}" || true

          echo "::error::GovAI hosted compliance gate failed: verdict=${first:-unknown}"
          exit 1

  upload_evidence_packs:
    name: upload_evidence_packs
    runs-on: ubuntu-latest
    needs: [evidence_pack]
    if: ${{ needs.evidence_pack.result == 'success' }}
    steps:
      - name: Prepare download dir
        run: |
          set -euo pipefail
          rm -rf "${GITHUB_WORKSPACE}/.aigov_ci_artifacts"
          mkdir -p "${GITHUB_WORKSPACE}/.aigov_ci_artifacts"

      - name: Download evidence artifacts
        uses: actions/download-artifact@v4
        with:
          name: evidence_packs
          path: ${{ github.workspace }}/.aigov_ci_artifacts

      - name: Verify downloaded artifacts
        run: |
          set -euo pipefail
          ls -la "${GITHUB_WORKSPACE}/.aigov_ci_artifacts" | head -n 200
          test -f "${GITHUB_WORKSPACE}/.aigov_ci_artifacts/keep.txt"

      - name: Upload evidence artifacts final
        uses: actions/upload-artifact@v4
        with:
          name: evidence_packs_final
          path: ${{ github.workspace }}/.aigov_ci_artifacts
          if-no-files-found: error
          include-hidden-files: true